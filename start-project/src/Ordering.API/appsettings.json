{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ServiceUrls": {
    "BasketApi": "https://localhost:7002",
    "_comment": "PROBLEM: Service dependencies create a configuration matrix. Ordering needs Basket, Basket needs Catalog. Each environment multiplies these configurations. With Aspire, dependencies are declared once in the AppHost."
  }
}
